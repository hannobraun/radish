def "{{moduleName}}", [], ( m ) ->
	nextEntityId = 0

	module =
		entityName: "{{name}}"

		createEntity: ( args ) ->
			id = nextEntityId
			nextEntityId += 1

			entity =
				id: "{{name}} " +id
				components:
					"exampleComponents": {}
