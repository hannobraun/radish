def 'Components', {{{modules}}}, ( m ) ->
	componentModuleNames = {{{modules}}}

	componentFactories = {}
	for moduleName in componentModuleNames
		module = m[ moduleName ]
		componentFactories[ module.componentName ] = module.createComponent

	module =
		create: ( component, args ) ->
			componentFactories[ component ]( args )
