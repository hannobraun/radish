#!/usr/bin/env node

require( "../output/tools.js" )


load( "GenerateImagePaths" ).generateImagePaths()
load( "GenerateAggregates" ).generateAggregate( "component" )
load( "GenerateAggregates" ).generateAggregate( "entity" )
load( "GenerateAggregates" ).generateAggregate( "system" )
