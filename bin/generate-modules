#!/usr/bin/env node

require( "../output/tools.js" )


load( "GenerateImagePaths" ).generateImagePaths()
load( "GenerateAggregates" ).generateAggregate( "components" )
load( "GenerateAggregates" ).generateAggregate( "entities" )
load( "GenerateAggregates" ).generateAggregate( "systems" )
