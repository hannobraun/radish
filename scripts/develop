coffee --watch --compile --join output/game.js \
	sources/modules \
	sources/code &
coffee --watch --compile --join output/test.js \
	sources/modules \
	sources/code \
	test/modules \
	test/code &

read
